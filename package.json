{
  "name": "superarticles",
  "version": "1.0.0",
  "description": "Interactive Superhero Articles Platform with Immersive Experience",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'No build step required'",
    "cleanup": "node -e \"import('./api/cleanup.js').then(m => m.handler({method: 'POST', headers: {'x-cron-secret': process.env.CRON_SECRET}}, {status: (code) => ({json: (data) => console.log(data)})}))\"",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "helmet": "^7.0.0",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "superhero",
    "articles",
    "interactive",
    "immersive",
    "vercel",
    "supabase"
  ],
  "author": "SuperArticles Team",
  "license": "MIT"
}
